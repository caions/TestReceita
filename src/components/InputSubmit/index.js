import React from 'react'
import {InputSubmit_} from './styles'

function InputSubmit({type,value}) {
    return (
        <>
        <InputSubmit_ type={type} value={value}/>

        </>
    )
}

export default InputSubmit